/**
 * 创建getters.js文件
 * 用于获取store.js文件中的参数
 * $store.getters.xxx
 */
export const userLogin = (state) => state.userLogin;
export const isLogin = (state) => state.isLogin;

export const courseIn = (state) => state.courseIn;
export const isInCourse = (state) => state.isInCourse;
